# You will need a .env.local file for local dev server (localhost)
# You will need a .env.test.local file for running the integration tests.

# Create these files, and add the following variables.
# The local files are ignored by default.

# In .env.local and .env.test.local:
NEXT_PUBLIC_SUPABASE_URL="https://XXXX.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="XXXX"

# AI Provider Keys (add the ones you plan to use)
NEXT_PUBLIC_AI_PROVIDER="openai" # Default provider: openai, anthropic, etc.
NEXT_PUBLIC_AI_FALLBACK_PROVIDER="anthropic" # Fallback provider if primary fails

# In addition to that, you also need .env.test.local with these variables:
SUPABASE_SERVICE_KEY="XXXX"

# Stripe Configuration
STRIPE_SECRET_KEY="sk_test_XXX"
STRIPE_PRICE_ID="price_XXX" # Premium tier price ID
STRIPE_BASIC_PRICE_ID="price_XXX" # Basic tier price ID (if applicable)
STRIPE_WEBHOOK_SECRET="whsec_XXX"

# Analytics (optional)
NEXT_PUBLIC_ANALYTICS_PROVIDER="plausible" # Options: plausible, ga4, etc.
NEXT_PUBLIC_ANALYTICS_SITE_ID="project-mosaic.com"
